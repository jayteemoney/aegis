# dependencies
/node_modules
/ui/node_modules

# frontend build output
/ui/dist
/ui/.vite

# Compact build artifacts (regenerated by `yarn compile`)
/contracts/managed
# The same artifacts, republished for the browser by `yarn zk:copy`
/ui/public/zk

# logs & local state
/logs
/midnight-level-db
/.midnight-expert

# ── SECRETS: never commit ──────────────────────────────────────────────
# Deployed contract addresses / deploy output
deployment.json
# Wallet seeds, mnemonics and any network credentials
.env
.env.*
!.env.example
!.env.*.example
*.seed
*.mnemonic
seed.txt
wallet-seed*
# ───────────────────────────────────────────────────────────────────────

# editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
